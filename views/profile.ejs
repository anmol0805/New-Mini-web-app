<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-black text-white">
    <div class="container mx-auto py-8">
        <div class="flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm " href="/logout">Logout</a>
        </div>
        <h1 class="text-3xl font-bold mb-4">Hello, <%= user.name %>👋</h1>
        <p class="mb-4">You can create a new post.</p>
        <form action="/post" method="post">
            <textarea placeholder="What's on your mind?" name="content" rows="3"
                class="w-[50%] bg-transparent block p-2 mb-4 outline-none border-[1.2px] border-zinc-700"></textarea>
            <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
                Create New Post
            </button>
        </form>
        <div class="mt-20">
            <h2 class="text-xl font-bold mb-2">Your Posts:</h2>
            <div class="postcontainer mb-1">

                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 p-1 border-2 border-zinc-700 bg-zinc-800 rounded-md mb-2">
                        <h4 class="text-blue-500 ">@<%=user.username %>
                        </h4>
                        <p class="text-sm tracking-tight">
                            <%= post.content %>
                        </p>
                        <small class="mt-2 text-xs text-blue-400 inline-block"><%= post.likes.length%> likes</small>
                        <div class="flex  gap-2 ">
                            <a  class="text-blue-400 text-xs" href="/like/<%= post._id %>">
                                <%=post.likes.indexOf(user._id) === -1 ? "like" : "unlike" %>
                            </a>
                            <a  class="text-zinc-400 text-xs" href="/edit/<%=post._id%>" >edit</a>

                        </div>
                    </div>
                    <% }) %>
            </div>
        </div>
    </div>
    </div>
</body>

</html>